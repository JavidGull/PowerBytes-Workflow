pipeline {
    agent none
    stages {
        stage('git') {
          agent { label "Develop"}
            steps {
              echo 'to build the code..             
            }
        }
     }
}

