pipeline {
    agent none
    stages {
        stage('build') {
          agent { label "Jenkins_Develop"}
            steps {
              echo 'to build the code..'             
            }
        }
     }
}

